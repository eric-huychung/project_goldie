/**
 * Analyze workspace route placeholder (Phase 3).
 */

import { WorkspacePlaceholder } from "@/components/workspace/workspace_placeholder";

/**
 * Analyze tab mirror — coming in Phase 3.
 */
export default function AnalyzePage() {
  return (
    <WorkspacePlaceholder
      title="Analyze"
      phase_label="Phase 3 — core architecture mirror."
    />
  );
}
