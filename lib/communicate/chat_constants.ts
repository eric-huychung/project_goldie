/**
 * Communicate chat session limits and model configuration.
 */

/** Maximum user messages per chat session (client and API enforce). */
export const CHAT_MAX_USER_MESSAGES = 5;

/** Gateway model id (low-cost, supports tools). */
export const CHAT_GATEWAY_MODEL_ID = "google/gemini-2.5-flash-lite";
