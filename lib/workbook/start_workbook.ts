/**
 * Entry point when user starts from home (template, blank, or prompt). Wired in Phase 2.
 */

import type { workbook_start } from "@/lib/types/template";

/** @param _start - How the user chose to begin */
export function start_workbook(_start: workbook_start): void {
  // Phase 2: navigate to /workspace with workbook context
}
